using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Microsoft.VisualStudio.Shell;
using Microsoft.VisualStudio.Shell.Interop;

namespace Pundit.Vsix
{
   static class VsixUtils
   {
      /*
      public static string GetSolutionDirectory(Package vsPackage)
      {
                  IVsSolution solution = vsPackage.GetService(typeof(SVsSolution)) as IVsSolution;

         if(solution != null)
         {

            solution.GetProperty(__VSPROPID.VSPROPID_SolutionDirectory)
         }

      }
      */
   }
}

@using Pundit.Core.Model
@model IEnumerable<Package>
@{
    ViewBag.Title = "Packages";
}

<h2>There are @ViewBag.Count package(s)</h2>

<table>
   @foreach (Package p in Model)
   {
   <tr>
      <td>@p.PackageId @p.VersionString (@p.Platform)</td>
   </tr>
   }
</table>
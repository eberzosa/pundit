@using Pundit.Core
@using Pundit.Core.Server.Model
@model IEnumerable<DbPackage>
@{
    ViewBag.Title = "Packages";
}

@*Html.ActionLink("publish", "Publish")*@

<h2>History (last 100 submissions)</h2>

<table>
   <tr>
      <th>PackageId</th>
      <th>Version</th>
      <th>Platform</th>
      <th>Created</th>
      <th>Size</th>
      <th></th>
   </tr>
   @foreach (DbPackage p in Model)
   {
      <tr>
         <td>@p.Package.PackageId</td>
         <td>@p.Package.Version</td>
         <td>@p.Package.Platform</td>
         <td>@p.CreatedDate</td>
         <td>@ByteFormat.ToString((ulong)p.FileSize)</td>
         <td>@Html.ActionLink("download", "DownloadPackage", new { id = p.Package.PackageId, v = p.Package.VersionString, p = p.Package.Platform })</td>
      </tr>
   }
</table>